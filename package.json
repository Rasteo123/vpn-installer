{
  "name": "vpn-installer",
  "version": "0.1.0",
  "description": "VPN stack installer for Ubuntu VPS + OpenWrt router",
  "author": "VPN Installer",
  "private": true,
  "main": "src/main/main.js",
  "scripts": {
    "test": "node --test",
    "snapshot": "node tools/snapshot.js",
    "dev": "electron .",
    "dist": "electron-builder",
    "dist:win": "electron-builder --win --x64",
    "dist:mac": "electron-builder --mac",
    "dist:linux": "electron-builder --linux"
  },
  "dependencies": {
    "ssh2": "^1.16.0"
  },
  "devDependencies": {
    "electron": "^32.3.3",
    "electron-builder": "^25.1.8"
  }
}
